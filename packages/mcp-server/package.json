{
  "name": "agent-treasury-mcp-server",
  "version": "0.1.0",
  "description": "Local MCP adapter exposing only the agent-scoped agent-treasury interface",
  "type": "module",
  "bin": {
    "agent-treasury-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "agent-treasury-sdk": "0.1.0",
    "zod": "^4.4.3"
  }
}

